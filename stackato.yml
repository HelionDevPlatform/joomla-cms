name: joomla
instances: 1
framework:
  type: php
mem: 128
services:
  ${name}-fs: filesystem
  ${name}-db: mysql
hooks:
  post-staging:
    # setting up in filesystem
    - sh stackato-setup.sh
    - echo $VCAP_SERVICES | json mysql | json -a credentials
